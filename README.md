# Gee (Go Echo server Example)
Gee is is an example of a server program created using golang echo framework.
</br>
</br>
## How to run
### Docker
```
docker-compose -f local.yml build
docker-compose -f local.yml up
```
